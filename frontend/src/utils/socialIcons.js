/**
 * Shared social platform → React Icon component map.
 * Import this instead of re-declaring per-file.
 */
import {
  FaEnvelope,
  FaLinkedin,
  FaGithub,
  FaInstagram,
  FaTwitter,
} from "react-icons/fa";

export const socialIconMap = {
  Email: FaEnvelope,
  LinkedIn: FaLinkedin,
  GitHub: FaGithub,
  Instagram: FaInstagram,
  Twitter: FaTwitter,
};
